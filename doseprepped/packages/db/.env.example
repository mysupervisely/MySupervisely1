# Copy to packages/db/.env so Prisma CLI commands (migrate, seed, studio)
# run from this package can reach your local database.
DATABASE_URL="postgresql://doseprepped:changeme@localhost:5432/doseprepped_dev"

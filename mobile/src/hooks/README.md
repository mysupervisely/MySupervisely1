# hooks/

Shared React hooks. Empty in M1. Expected early tenants once content/storage land:
`useContentRepository`, `useExamEngine`, `useAccessToken` — thin hooks wrapping the `services/`
layer for screen consumption.

# Copy to .env for local `netlify dev`. Never commit the real .env file.
# Set these in Netlify (Site configuration -> Environment variables) for deploys.

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
SUPABASE_URL=
SUPABASE_SECRET_KEY=
